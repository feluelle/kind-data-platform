version: "3"

includes:
  base: ../Taskfile.yml

vars:
  SERVICE: airflow

tasks:
  default:
    cmds:
      - task: base:docker:build
      - task: base:kind:load
      - terraform -chdir=../infra apply -replace="helm_release.airflow"
